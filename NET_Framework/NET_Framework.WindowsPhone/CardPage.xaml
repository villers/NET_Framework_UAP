<Page
    x:Class="NET_Framework.CardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NET_Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Card, Source={StaticResource Locator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <ScrollViewer>
        <StackPanel Grid.Row="0" Margin="10,0,10,0">
            <TextBlock Text="PConfig" Style="{StaticResource HeaderTextBlockStyle}" />
            <TextBlock Text="Show Card" Style="{StaticResource BaseTextBlockStyle}"/>

            <Button Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="0">
                <Button.Content>
                    <StackPanel>
                        <TextBlock Text="{Binding ComponentSelected.Graphic.Title}"/>
                        <Image Source="{Binding ComponentSelected.Graphic.Image}" />
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
    </ScrollViewer>
</Page>
